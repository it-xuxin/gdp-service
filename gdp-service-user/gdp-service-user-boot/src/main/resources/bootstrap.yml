spring:
  application:
    name: gdp-service-user-boot
  profiles:
    active: dev

server:
  port: 8801

logging:
  level:
    root: info
knife4j:
  # 开启增加配置
  enable: true
  # 开启生产环境屏蔽
#  production: false
  # 开启Swagger的Basic认证功能,默认是false
  basic:
    enable: false
    # Basic认证用户名
    username: test
    # Basic认证密码
    password: 123